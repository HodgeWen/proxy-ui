import * as React from "react"
import { Separator as HeroSeparator } from "@heroui/react"

export function Separator(props: React.ComponentPropsWithoutRef<typeof HeroSeparator>) {
  return <HeroSeparator {...props} />
}
